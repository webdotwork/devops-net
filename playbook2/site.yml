---
# - name: Install Clickhouse
#   hosts: clickhouse
#   handlers:
#     - name: Start clickhouse service
#       become: true
#       ansible.builtin.service:
#         name: clickhouse-server
#         state: restarted
#   tasks:
#     - block:
#         - name: Get clickhouse distrib
#           ansible.builtin.get_url:
#             url: "https://packages.clickhouse.com/rpm/stable/{{ item }}-{{ clickhouse_version }}.noarch.rpm"
#             dest: "./{{ item }}-{{ clickhouse_version }}.rpm"
#           with_items: "{{ clickhouse_packages }}"
#       rescue:
#         - name: Get clickhouse distrib
#           ansible.builtin.get_url:
#             url: "https://packages.clickhouse.com/rpm/stable/clickhouse-common-static-{{ clickhouse_version }}.x86_64.rpm"
#             dest: "./clickhouse-common-static-{{ clickhouse_version }}.rpm"
#     - name: Install clickhouse packages
#       become: true
#       ansible.builtin.yum:
#         name:
#           - clickhouse-common-static-{{ clickhouse_version }}.rpm
#           - clickhouse-client-{{ clickhouse_version }}.rpm
#           - clickhouse-server-{{ clickhouse_version }}.rpm
#       notify: Start clickhouse service
#     - name: Create database
#       ansible.builtin.command: "clickhouse-client -q 'create database logs;'"
#       register: create_db
#       failed_when: create_db.rc != 0 and create_db.rc !=82
#       changed_when: create_db.rc == 0


- name: Vector
  hosts: vector
  tasks:
    - name: Download Vector arch
      ansible.builtin.get_url:
        url: https://packages.timber.io/vector/0.23.0/vector-0.23.0-x86_64-unknown-linux-gnu.tar.gz
        dest: ./vector-0.23.0-x86_64-unknown-linux-gnu.tar.gz

    - name: Dir for Vector arch
      become: true
      ansible.builtin.file: 
        path: /opt/vector
        state: directory
        owner: ans
        group: ans
        mode: '0776'
        
    # - name: Install gzip
    #   become: true
    #   ansible.builtin.apt:
    #     name: unzip

    - name: Extract Vector arch
      become: true
      ansible.builtin.unarchive:
        src: /home/ans/vector-0.23.0-x86_64-unknown-linux-gnu.tar.gz
        dest: /opt/vector
        remote_src: true

    - name: Create vector bin link
      become: true
      ansible.builtin.file:
        src: /opt/vector/vector-x86_64-unknown-linux-gnu/bin
        dest: /usr/bin/
        state: link

    # - name: mv vector bin
    #   become: true
    #   ansible.builtin.command:
    #     mv /usr/bin/vector /usr/bin

    - name: Install Vector
      become: true
      ansible.builtin.command:
        vector
      